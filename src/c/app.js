import Actor from "../m/Actor.js";
import Movie from "../m/Movie.js";
import Producer from "../m/Producer.js";

function generateTestData(){
    try{
        
    }catch (e) {
        console.log( `${e.constructor.name}: ${e.message}`);
    }
}